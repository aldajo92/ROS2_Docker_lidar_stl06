version: '3.8'

services:
  ros2_container:
    image: ${DOCKER_REGISTRY_ADDR}/${DOCKER_IMAGE_NAME}:latest
    container_name: ${DOCKER_CONTAINER_NAME}
    privileged: true
    environment:
      - ROS_DOMAIN_ID=0
    volumes:
      - /dev/:/dev
      - /run/udev:/run/udev
    networks:
      default:
        aliases:
          - ${DOCKER_NETWORK}
    command: ["/bin/bash", "-c", "source /ros2_ws/install/setup.bash; ros2 launch waver_physical_bringup main.launch.py"]
    restart: always
    
